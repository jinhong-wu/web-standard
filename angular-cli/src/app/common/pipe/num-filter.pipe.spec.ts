import { NumFilterPipe } from './num-filter.pipe';

describe('NumFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NumFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
