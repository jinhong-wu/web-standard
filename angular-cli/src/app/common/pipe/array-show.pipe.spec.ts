import { ArrayShowPipe } from './array-show.pipe';

describe('ArrayShowPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayShowPipe();
    expect(pipe).toBeTruthy();
  });
});
